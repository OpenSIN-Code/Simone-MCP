Simone-MCP is a Python-first MCP 2025-11-25 code worker. Full source under src/simone_mcp/ with: protocol.py (central MCP handler with Tasks, structured output, resource_link), core.py (LibCST/Jedi/tree-sitter engines + TOOL_DEFINITIONS with outputSchema), http_app.py (FastAPI + streamable HTTP), mcp_stdio.py (stdio transport), schemas.py (Pydantic models), correlation.py (tool call tracking), a2a_handler.py (A2A JSON-RPC), hybrid_memory.py (Qdrant + Neo4j). Tests: 38/38 passing. MCP features: tasks/get, tasks/result, tasks/list, tasks/cancel, structuredContent + outputSchema on all tools, execution.taskSupport (forbidden/optional/required), resource_link type, SEP-1303 input validation, _meta propagation, logging notifications, SSE retry, MCP-Protocol-Version header, session cleanup on DELETE. Tool names: sin_simone_mcp_* namespace.
