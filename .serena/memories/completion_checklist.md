When finishing work on Simone-MCP: 1) run python3 -m pytest tests/test_simone_mcp.py -q, 2) run python3 src/cli.py print-card, 3) run python3 src/cli.py run-action '{"action":"simone.mcp.health"}', 4) run a stdio smoke test like printf '{"jsonrpc":"2.0","id":1,"method":"initialize"}\n' | python3 src/cli.py serve-mcp, 5) import the HTTP app via python3 -c "from src.main import app; print(app.title)", 6) optionally smoke-test HTTP endpoints with FastAPI TestClient, 7) review git status for modified/untracked files and call out any machine-local config such as .opencode/ before committing.