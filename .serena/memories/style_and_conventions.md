Current repo conventions: keep Python source in src/simone_mcp/. Both stdio and streamable HTTP MCP compatibility via shared protocol.py handler. Tool names use sin_simone_mcp_* namespace (sin_simone_mcp_health, sin_simone_mcp_symbol_search, sin_simone_mcp_find_references, sin_simone_mcp_structural_edit, sin_simone_mcp_memory_query, sin_simone_mcp_project_overview). Legacy code.* names still work as aliases in _SYNC_ACTIONS. MCP protocol version 2025-11-25. execution.taskSupport uses string values (forbidden/optional/required), not boolean. inputSchema and outputSchema both use $schema: JSON Schema 2020-12. Keep tests import-compatible with src/mcp_server.py shim. Avoid unnecessary comments/docstrings. Keep metadata aligned across AGENTS.md, README.md, A2A-CARD.md, and .serena memories.
