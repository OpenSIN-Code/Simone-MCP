"""Simone MCP — top-level package marker.

The `src/` directory is the repository's source root; the actual
package lives at `src/simone_mcp/`. This file is intentionally empty
so that `import src` and `import simone_mcp` both work.

Docs: __init__.doc.md
"""